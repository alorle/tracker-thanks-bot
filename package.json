{
  "name": "tracker-thanks-bot",
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": ">=24.16.0"
  },
  "scripts": {
    "build": "tsc",
    "start": "node src/index.ts",
    "test": "npm run build && node --test --test-timeout=120000 test/e2e.test.js"
  },
  "license": "MIT",
  "dependencies": {
    "playwright": "^1.60.0",
    "prom-client": "^15.1.3"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3"
  }
}
